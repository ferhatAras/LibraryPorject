@using KutuphaneMvc.Models.Entities;
@model List<TBLDUYURU>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Layout.cshtml";
    string renk = "";
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<h2>Duyuru</h2>
<table class="table table-hover">
    <tr style="background-color: #00215E;color:#fff">
        <th>DUYURU ID</th>
        <th>KATEGORİ</th>
        <th>TARİH</th>
        <th>SİL</th>
        <th>İÇERİK</th>
    </tr>
    @foreach (var prs in Model)
    {

        //if (prs.KATEGORI == "Diğer")
        //{
        //    renk = "bg-primary text-white";
        //}
        //if (prs.KATEGORI == "Yeni Kitap")
        //{
        //    renk = "bg-warning text-white";
        //}
        //if (prs.KATEGORI == "İletişim")
        //{
        //    renk = "bg-success text-white";
        //}
        //if (prs.KATEGORI == "Kitapları Geciktirmeyin")
        //{
        //    renk = "bg-danger text-white";
        //}
        <tr>
            <td>@prs.ID</td>
            <td >@prs.KATEGORI</td>
            <td>@Convert.ToDateTime(prs.TARIH).ToString("dd/MM/yyyy")</td>
            <td> <a href="/Duyuru/DuyuruSil/@prs.ID" class="btn btn-outline-danger btn-lg">Sil</a> </td>

            <td> <a href="/Duyuru/DuyuruBilgiler/@prs.ID" class="btn btn-outline-primary btn-lg">Bilgiler</a> </td>
        </tr>

    }

</table>
<a href="/Duyuru/YeniDuyuru/" class="btn btn-primary btn-lg">Yeni Duyuru</a>

